# kotlin-lotto-precourse

## 프로젝트 소개

이번 프로젝트는 랜덤한 숫자로 구성된 n개의 로또와 당첨 번호를 비교해 수익률을 계산하는 프로그램입니다.

## 용어 정리

| 용어     | 의미                             | 
|--------|--------------------------------|
| 로또     | 6개의 로또 번호를 가집니다                |
| 당첨 번호  | 정규 번호와 보너스 번호로 구성됩니다           |
| 정규 번호  | 6개의 로또 번호를 가집니다                |
| 보너스 번호 | 1개의 로또 번호를 가집니다                |
| 로또 결과  | 당첨 번호와 로또의 비교 결과를 가집니다         |
| 당첨 결과  | 로또 결과를 바탕으로 당첨 결과를 가집니다 |

## 주요 요소 정리

| 요소               | 의미                               | 
|------------------|----------------------------------|
| `PurchaseResult` | 로또를 몇 개 구매했는 지 저장합니다             |
| `Lotto`          | 로또 번호를 저장합니다                     |
| `WinningNumbers` | 당첨 번호를 저장하며 정규 번호와 보너스 번호로 구성됩니다 |
| `RegularNumbers` | 정규 번호를 저장합니다                     |
| `Bonus`          | 보너스 번호를 저장합니다                    |
| `LottoResult`    | 로또와 당첨 번호의 비교 결과를 저장합니다          |
| `Rank`           | 로또 결과를 바탕으로 등수를 판단합니다            |
| `RankResult`     | 당첨 결과를 저장합니다                     |
| `Profit`         | 로또 구매 개수와 당첨 결과를 바탕으로 수익률을 계산합니다 |

## 주요 진행 흐름

1. 돈 단위의 유효성을 판단해 로또의 개수를 생성합니다(`PurchaseResult`)
2. 로또(`Lotto`)를 `PurchaseResult`가 저장한 개수만큼 생성합니다
3. 당첨 번호를 생성합니다(`WinningNumbers`)
    - 유효한 정규 번호를 만듭니다(`RegularNumbers`)
    - 유효한 보너스 번호를 만듭니다(`Bonus`)
4. 당첨 번호와 구매한 로또를 비교합니다(`LottoResult`)
5. 로또 결과(`LottoResult`)를 바탕으로 `Rank`를 통해 당첨 결과(`RankResult`)을 얻습니다
6. 얻어진 당첨 결과(`RankResult`)와 로또 구매 개수(`PurchaseResult`)를 통해 수익률을 계산합니다.(`Profit`)

## 구현할 기능 목록

- [x] 로또 번호의 숫자 범위가 유효한 범위인지 확인한다
- [x] 당첨 번호의 숫자 범위가 유효한 범위인지 확인한다
- [x] 보너스 번호가 유효한 범위인지 확인한다
- [x] 로또 번호들이 중복되지 않은 숫자로 구성되었는 지 확인한다
- [x] 당첨 번호들이 중복되지 않은 숫자로 구성되었는 지 확인한다
- [x] 당첨 번호와 보너스는 중복되지 않았는 지 확인한다
- [x] 로또 번호를 정해진 개수만큼 뽑는다
    - 정해진 수량보다 적게 뽑는 경우
    - 정해진 수량보다 많이 뽑는 경우
- [x] 당첨 번호를 정해진 개수만큼 뽑는다
    - 보너스 번호를 정해진 개수만큼 뽑는다
- [x] 구입 금액이 유효한 단위가 아니라면 예외 처리한다
    - 소수로 구입 금액을 입력하는 경우
    - 숫자가 아닌 값을 입력하는 경우
    - 음수를 입력하는 경우
- [x] 아무것도 구입하지 않는 경우는 예외 처리한다
- [x] 구입 금액에 해당하는 만큼 로또를 발행한다
- [x] 사용자의 입력에 해당하는 만큼 구매 단위를 계산한다
    - 요구사항에 맞는 출력 메시지를 표시한다
    - 잘못된 입력이 들어오면 에러를 출력 후 다시 입력받는다
- [x] 랜덤한 숫자로 구성된 로또를 발행한다
- [x] 구매한 개수만큼 랜덤한 숫자로 구성된 로또를 발행한다
- [x] 로또는 오름차순으로 정렬해 보여준다
- [x] 정규 번호를 사용자로부터 입력받는다
    - 요구사항에 맞는 출력 메시지를 표시한다
- [x] 보너스 번호를 사용자로부터 입력받는다
    - 요구사항에 맞는 출력 메시지를 표시한다
- [x] 정규 번호와 보너스 번호를 입력받아 중복 확인 후 당첨 번호를 만든다
- [x] 당첨 번호와 구매한 로또를 비교한다
- [x] 비교 결과를 통해 등수를 판단한다
- [x] 구매한 모든 로또들에 대한 당첨 내역을 출력한다
- [x] 구매한 모든 로또를 통해 수익률을 계산한다
- [x] 계산한 수익률을 출력한다
    - 수익률은 소수점 둘째 자리에서 반올림한다.(ex. 100.0%, 51.5%)
- [x] 예외 상황 시 에러 문구는 `[ERROR]`로 시작한다

## 결정한 요구 사항

### 1. 당첨 번호와 보너스 번호는 중복되지 않아야 한다.

2등 조건이 5개의 번호 + 보너스 번호 일치입니다.
만약 당첨 번호와 보너스가 중복된다면 보너스 번호 일치 시 5개의 번호가 아닌 6개의 번호가 일치하는 1등이 됩니다.
따라서 1등과 2등을 구분시키기 위해 당첨 번호는 보너스 번호와 중복되어서는 안됩니다.
